hooks:
  before:
    - echo top before hook
forwards:
  app: app
options:
  config:
    path: config.json
  secrets:
    path: secrets.json
  environment:
    ENVIRONMENT_OPTION: top-level value
    TOP_LEVEL_OPTION: top-level option
